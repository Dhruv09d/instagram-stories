import { Styles } from "types/common";

const styles: Styles = {
  wrapper: {
    // maxWidth: "500px",
    margin: "auto",
  },

  story: {
    height: "100%",
    width: "100%",
    backgroundColor: "text.white",
  },
};

export default styles;
